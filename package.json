{
  "name": "tenor",
  "private": true,
  "version": "0.1.0",
  "description": "Adversarial underwriting for freelancer receivables",
  "license": "MIT",
  "workspaces": [
    "agents",
    "web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "samples": "npm run samples --workspace agents",
    "price": "npm run price --workspace agents",
    "spread": "npm run spread --workspace agents",
    "verify": "npm run verify --workspace agents"
  }
}
